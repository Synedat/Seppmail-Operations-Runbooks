# Example outputs

This document provides example output snippets for **Seppmail-Operations-Runbooks** so that readers can understand what a successful run, report or engineering handover may look like.

## JSON sample
```json
{"runbook":"mail-queue-backlog","severity":"high","mitigation":"traffic-shaping-enabled"}
```

## YAML sample
See `demo-data/sample-config.yaml`.

## CSV sample
See `demo-data/sample-health-report.csv`.

## How to use these outputs
- Add them to screenshots and GitHub Pages examples.
- Use them in workshops when no customer system is available.
- Reuse them in issue templates, changelog entries and release notes.
- Replace placeholder values with environment-specific identifiers during delivery.

## Presentation guidance
Keep sample output small, readable and realistic. Prefer evidence-oriented fields such as timestamps, result states, identifiers, severity and next actions.
